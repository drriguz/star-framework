---
description: Implement a feature spec test-first (TDD)
agent: star-implementer
---

Implement the feature named in the user's message. Find its spec in `specs/` (confirm which one if there are several), then follow the implementer procedure: failing tests first, then minimal implementation, spec section by section, keeping the suite green. The spec is the source of truth — if it is wrong or ambiguous, stop and ask to update it.
